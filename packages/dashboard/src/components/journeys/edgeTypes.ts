import PlaceholderEdge from "./edgeTypes/placeholderEdge";
import WorkflowEdge from "./edgeTypes/workflowEdge";

const edgeTypes = {
  placeholder: PlaceholderEdge,
  workflow: WorkflowEdge,
};

export default edgeTypes;
