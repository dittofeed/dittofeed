name: Dashboard CI

on:
  push:
    branches:
      - main
    paths:
      - 'packages/dashboard/**'
  pull_request:
    branches:
      - main
    paths:
      - 'packages/dashboard/**'
  workflow_dispatch:

jobs:
  lint-dashboard:
    uses: ./.github/workflows/lint-workflow.yml
    with:
      package-name: 'dashboard'
